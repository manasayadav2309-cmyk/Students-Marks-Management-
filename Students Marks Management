#include <stdio.h>
int main() {
    int marks[5], total = 0;
    float avg;

    for(int i=0; i<5; i++) {
        scanf("%d", &marks[i]);
        total += marks[i];
    }
    avg = total / 5.0;

    printf("Total = %d\nAverage = %.2f", total, avg);
    return 0;
}
